export type JSelectProps = {
    options: string[],
    defaultIndex: number,
    primaryColor: string,
    textColor: string,
    width: string,
    returnFunction: Function
}

export type JCheckboxProps = {
    defaultState: boolean,
    primaryColor: string,
    backgroundColor: string,
    returnFunction: Function
}